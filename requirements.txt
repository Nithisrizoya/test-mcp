# Groq API Client
groq>=0.4.0

# Model Context Protocol (MCP)
mcp>=0.6.0

# Environment variable management
python-dotenv>=1.0.0

# Async HTTP client
aiohttp>=3.9.0

# Optional: For Node.js compatibility
# Ensure Node.js and npm are installed separately
